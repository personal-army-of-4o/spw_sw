env:
  global:
    - IMG=gcc
    - DISTR=latest
    - SCRIPT=make test_all
    - WORKDIR=/mnt/data

services:
  - docker

# Here are the list of run scripts of different DUTs/testcases
script:
  - >
    docker run -t -v `pwd`:$WORKDIR $IMG:$DISTR
    /bin/bash -c "cd $WORKDIR; $WORKDIR/$SCRIPT"

